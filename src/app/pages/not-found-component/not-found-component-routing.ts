import { Routes } from '@angular/router';
import { NotFoundComponentComponent } from './not-found-component.component';

export const NotFoundComponentRoutes: Routes = [
    { path: '',component: NotFoundComponentComponent }
];
